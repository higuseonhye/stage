/**
 * Supabase clients are split so `next/headers` stays server-only:
 * - `@/lib/supabase/browser` — Client Components
 * - `@/lib/supabase/server` — Server Components, Route Handlers, Server Actions
 */

export {};
